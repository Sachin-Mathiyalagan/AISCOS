# AISCOS Frontend Environment Variables
# If deploying frontend separately (e.g. Vercel/Netlify), set this to your backend public URL (e.g., https://aiscos-backend.onrender.com)
# Leave empty if running locally with Vite proxy or via Docker container
VITE_API_URL=
